define(['modules/page'], function (page) {
  describe('page', function () {
    var fixture, popup, popupPageHTML;

    describe('#initialization', function () {
      it('listens on window, and respons to page events', function () {
      });
    });
  });
});
